# KyberM0nk TODO LIST

## Epic: Multi-Machine Engine Orchestration (Cross-Platform Bridge)
**Goal:** Enable Kyber ecosystem agents (Linux/Docker) to issue build commands and interact directly with Unreal Engine 5 on the Windows host (`14700K`).

- [x] **SSH Tunnel Validation:** Confirm passwordless SSH from the Kyber Linux box and Docker Sandbox into the Windows machine `unreal-windows`. *(Validated!)*
- [x] **Toolchain Discovery:** Locate MSBuild path on Windows. *(Found at `C:\Program Files\Microsoft Visual Studio\2022\Community\MSBuild\Current\Bin\MSBuild.exe`)*
- [x] **CrewAI MSBuild Tool:** (Installed via ssh in web container) Create an `UnrealMSBuildTool` that allows the Kyber Main Quest agents to trigger `.sln` and UnrealBuildTool (UBT) builds remotely via SSH/PowerShell.
- [x] **Unreal Python Bridge:** Configure Unreal Engine's Web Remote Control / Python Remote execution API on the Windows machine.
- [x] **CrewAI Unreal Tool:** Create a custom CrewAI tool that takes Python scripts generated by our local LLMs and natively executes them inside the running Unreal Editor on Windows.
- [x] **Agent Tool Registration:** Bind these Tools to the *"Kyber Ecosystem Architect"* and the *"Main Quest Project Manager"* in CrewAI-Studio.

