# Continue Config

Continue remains a VS Code extension, not a KyberM0nk container.

This directory is for provider snippets and notes only.

Target endpoint:

```text
http://127.0.0.1:11434/v1
```

Initial model alias:

```text
qwen3-35b-uncensored
```
