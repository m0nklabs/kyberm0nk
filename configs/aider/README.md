# Aider Config

Aider should act as the Scalpel layer.

Planned configuration:

- use Guardian `/v1` as OpenAI-compatible endpoint
- use `DEFAULT_MODEL` or `FAST_MODEL` depending on task size
- operate on one active project at a time
- keep patches focused and testable
